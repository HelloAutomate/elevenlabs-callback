# --- Required ---
ELEVENLABS_API_KEY=sk-xxx
ELE_AGENT_ID=ag_xxx
TWILIO_FROM=+1XXXXXXXXXX
CALL_EVENTS_URL=https://YOUR-APP.up.railway.app/call-events

# --- GHL (optional but recommended for notes) ---
GHL_API_KEY=lc_xxx

# Optional shared secret for webhook auth (set same in GHL header X-Auth-Token)
SHARED_SECRET=demo-secret

# Port (Railway sets this automatically, but kept for local dev)
PORT=8080
